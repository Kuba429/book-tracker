export interface book {
    id: string;
    title: string;
    author: string;
    pages: number;
    language: string;
    coverPath: string;
}
export interface userDataInterface {
    id: string;
    email: string;
}
