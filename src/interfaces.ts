export interface book {
    id: string;
    title: string;
    author: string;
    pages: number;
    language: string;
    cover_path: string;
}
export interface userDataInterface {
    id: string;
    email: string;
}
